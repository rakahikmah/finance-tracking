meta {
  name: Create Category
  type: http
  seq: 1
}

post {
  url: {{url}}/api/v1/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Makan Malam"
  }
}
